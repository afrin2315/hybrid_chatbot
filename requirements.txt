# Serving dependencies (what the deployed app needs at runtime).
# Training-only packages live in requirements-train.txt.
flask==3.0.3
flask-cors==5.0.0
gunicorn==23.0.0
numpy==1.26.4
requests==2.32.3
scikit-learn==1.3.2
joblib==1.4.2
# Accurate tier (DistilBERT), PyTorch backend. The app also runs without these:
# the cascade degrades to the fast + safety tiers when the transformer is absent.
torch==2.5.1
transformers==4.36.0
